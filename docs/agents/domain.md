# Domain Docs

Single-context layout:

- `CONTEXT.md` at repo root — project-wide domain glossary and context
- `docs/adr/` at repo root — architectural decision records

No sub-contexts. All agents read from the root-level files.
